# 開発ガイドライン
- このプロジェクトではパッケージ管理と仮想環境の構築に `uv` を使用します。
- `pip` は使用せず、必ず `uv add <パッケージ名>` で追加し、実行時は `uv run` を使用してください。
- 作業が完了しテストが通ったら、必ず `git add .` -> `git commit` -> `git push` を行い、GitHubへ成果物を退避させてください。